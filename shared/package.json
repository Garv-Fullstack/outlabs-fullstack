{
  "name": "@reachinbox/shared",
  "version": "1.0.0",
  "description": "Shared types and enums for ReachInbox",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
